# yt-dlp global configuration for Railway deployment
# Loaded automatically by both the CLI subprocess and the Python YoutubeDL API.

# Enable EJS (External JavaScript runtime) for n-challenge / SABR solving.
# Deno is installed via nixpacks.toml and is on PATH at runtime.
# This is required since yt-dlp 2025.11.12 for reliable YouTube format extraction.
--remote-components ejs:github
